@startuml

class UserRepository {
    + <<create>> UserRepository (): UserRepository
    + addUser (user: User)
    + removeUser (screenName: String)
    - existsUser (screenName: String): Boolean
    + getUsers (): User [*]
}


class User {
    - screenName: String

    + <<create>> User (screenName: String): User
    + addTweet (text: String)
    + addRetweet (originalTweet: Tweet)
    + getPosts (): Post [*]
    + getScreenName (): String
}


abstract class Post <<abstract>> {
    + <<abstract>> getText (): String
}


class Tweet {
    -text: String

    + <<create>> Tweet (text: String): Tweet
    + getText (): String
    - cumpleCaracteres (text: String): Boolean
}


class Retweet {
    + <<create>> Retweet (originalTweet: Tweet): Retweet
    + getText (): String

}

UserRepository --> User : " - usuarios (0..*)"
User *--> Post : " - posts (0..*)"
Retweet --> Tweet : " - originalTweet "

class Tweet extends Post
class Retweet extends Post

@enduml